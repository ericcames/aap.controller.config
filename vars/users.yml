---
controller_user_accounts:
 - user: "{{ zigs_username }}"
   is_superuser: true
   password: "{{ zigs_passwd }}"
   email: "{{ zigs_email }}"
   first_name: "{{ zigs_firstname }}"
   last_name: "{{ zigs_lastname }}"

 - user: "{{ marks_username}}"
   is_superuser: true
   password: "{{ marks_passwd }}"
   email: "{{ marks_email }}"
   first_name: "{{ marks_firstname }}"
   last_name: "{{ marks_lastname }}"

 - user: "{{ chuas_username}}"
   is_superuser: true
   password: "{{ chuas_passwd }}"
   email: "{{ chuas_email }}"
   first_name: "{{ chuas_firstname }}"
   last_name: "{{ chuas_lastname }}"

 - user: "{{ leblancs_username}}"
   is_superuser: true
   password: "{{ leblancs_passwd }}"
   email: "{{ leblancs_email }}"
   first_name: "{{ leblancs_firstname }}"
   last_name: "{{ leblancs_lastname }}"

 - user: "{{ shads_username}}"
   is_superuser: true
   password: "{{ shads_passwd }}"
   email: "{{ shads_email }}"
   first_name: "{{ shads_firstname }}"
   last_name: "{{ shads_lastname }}"

 - user: "{{ worms_username}}"
   is_system_auditor: true
   password: "{{ worms_passwd }}"
   email: "{{ worms_email }}"
   first_name: "{{ worms_firstname }}"
   last_name: "{{ worms_lastname }}"

 - user: "{{ gils_username}}"
   is_superuser: true
   password: "{{ gils_passwd }}"
   email: "{{ gils_email }}"
   first_name: "{{ gils_firstname }}"
   last_name: "{{ gils_lastname }}"

 - user: "{{ lows_username}}"
   password: "{{ lows_passwd }}"
   email: "{{ lows_email }}"
   first_name: "{{ lows_firstname }}"
   last_name: "{{ lows_lastname }}"